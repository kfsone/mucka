name: Patch Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}

      - name: Compute and write release version
        id: ver
        run: |
          python3 - <<'PYEOF'
          import json, re, os

          with open('version.json') as f:
              v = json.load(f)
          current = v['version']

          new_version = re.sub(r'-rc\d+$', '', current)
          print(f"Releasing: {new_version}")

          v['version'] = new_version
          with open('version.json', 'w') as f:
              json.dump(v, f, indent=2)
              f.write('\n')

          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f"version={new_version}\n")
          PYEOF

      - name: Commit, tag, push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add version.json
          git commit -m "chore: release v${{ steps.ver.outputs.version }}"
          git tag -a "v${{ steps.ver.outputs.version }}" -m "Release ${{ steps.ver.outputs.version }}"
          git push
          git push origin "v${{ steps.ver.outputs.version }}"
