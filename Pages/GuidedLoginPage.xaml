<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Mucka.ViewModels"
             x:Class="Mucka.Pages.GuidedLoginPage"
             x:DataType="vm:GuidedLoginViewModel"
             Title="Guided Login"
             BackgroundColor="#0C0C0C"
             SafeAreaEdges="All">
    <Grid RowDefinitions="*,Auto" Padding="24">
        <Grid Grid.Row="0">
            <!-- Splash/banner rendered as a large, centered, dim backdrop: the persona-choice
                 action sheet appears on top of this page, so this is what shows behind it. Rows
                 are kept left-aligned within the block (NoWrap) so ASCII-art column alignment
                 isn't destroyed; only the whole block is centered. -->
            <ScrollView IsVisible="{Binding HasSplash}"
                        Orientation="Both"
                        HorizontalOptions="Center"
                        VerticalOptions="Center">
                <Label Text="{Binding SplashText}"
                       TextColor="#3a3a3a"
                       FontFamily="Courier"
                       FontSize="11"
                       LineBreakMode="NoWrap"/>
            </ScrollView>

            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="16">
                <ActivityIndicator IsRunning="True" Color="#1f6feb" HorizontalOptions="Center"/>
                <Label Text="{Binding Status}"
                       TextColor="#cccccc"
                       FontSize="16"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>

        <Button Grid.Row="1"
                Text="Cancel"
                Command="{Binding CancelCommand}"
                BackgroundColor="#2d333b"
                TextColor="#cccccc"
                CornerRadius="6"
                HeightRequest="48"
                Margin="0,16,0,0"/>
    </Grid>
</ContentPage>
